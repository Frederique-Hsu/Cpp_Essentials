/*!
 *  \file       Pointer.hpp
 *  \brief
 *
 */


#pragma once

void accessVoidPointer(int* pint);

const char* strchr(const char* p, char c);      /* 在字符串p中找到字符c第一次出现的位置 */
char* strchr(char* p, char c);
