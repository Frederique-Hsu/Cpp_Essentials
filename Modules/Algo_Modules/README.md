# C++20 Modules, CMake, and Shared libraries


## Linker symbol visibility

A common misconception among developers that:

> C++20 modules replace the need to handle symbol visibility, but that is not the case.

**Modules still require symbol visibility control, just like the non-module code.**